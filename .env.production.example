# Production Environment Variables
# Copy this to .env.production for production deployment

# Backend API URL (Render)
NEXT_PUBLIC_API_URL=https://smessage.onrender.com

# Note: ADMIN_PASSWORD is only used on the backend (Render)
# The frontend (Vercel) does not need this variable
